{
  "name": "vw-lease-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "sass scss:css -w --style compressed",
    "prefix": "postcss css --use --no-map autoprefixer -d css/prefixed"
  },
  "author": "",
  "browserslist": "last 4 versions",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^5.0.0",
    "sass": "^1.32.12"
  }
}
